requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.0
python-dateutil>=2.8.0
# scipy is NOT required - we use a pure-math Acklam inverse-normal in rop_calculator.
# Playwright is optional - only needed if requests-based scraper fails.
# Install with: pip install playwright && playwright install chromium
